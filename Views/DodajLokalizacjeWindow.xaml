<Window x:Class="EventManagementApp.DodajLokalizacjeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj Lokalizację" Height="300" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Nazwa" Margin="0,0,0,5"/>
            <TextBox x:Name="NazwaTextBox" Height="25"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="1">
            <TextBlock Text="Adres" Margin="0,0,0,5"/>
            <TextBox x:Name="AdresTextBox" Height="25"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,10" Grid.Row="2">
            <TextBlock Text="Pojemność Maksymalna" Margin="0,0,0,5"/>
            <TextBox x:Name="PojemnoscTextBox" Height="25"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center">
            <Button Content="Zapisz" Width="100" Click="ZapiszButton_Click"/>
            <Button Content="Anuluj" Width="100" Click="AnulujButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
