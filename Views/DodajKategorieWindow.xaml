<Window x:Class="EventManagementApp.DodajKategoriaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj Kategorię" Height="200" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nazwa Kategorii" Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Center"/>
        <TextBox x:Name="NazwaTextBox" Grid.Row="1" Margin="0,0,0,20" Height="25"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Dodaj" Click="DodajButton_Click" Width="100" Margin="10"/>
            <Button Content="Anuluj" Click="AnulujButton_Click" Width="100" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
